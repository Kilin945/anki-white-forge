[project]
name = "anki-word-adder"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "pyspellchecker>=0.9.0",
    "groq>=1.2.0",
    "edge-tts>=7.2.8",
    "duckduckgo-search>=7.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
